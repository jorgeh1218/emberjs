
  {{#each model as |item|}}
	  <section class="col-sm-12 color-group {{item.color}}-bricks">
			<h2> {{item.color}} </h2>
			{{#each item.bricks as |bricks|}}
					{{brick-item bricks=bricks sale=item.onSale}}
			{{/each}}
	  </section>
  {{/each}}
{{outlet}}
